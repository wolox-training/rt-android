<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/vNewsSwipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/vNewsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/spacing_none"
            android:paddingBottom="@dimen/static_spacing_medium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/vNewsFab"
        android:layout_width="@dimen/spinner_large"
        android:layout_height="@dimen/spinner_large"
        android:layout_margin="@dimen/static_spacing_large"
        android:src="@drawable/ic_tick"
        app:fab_colorNormal="@color/wolox_main_gren"
        app:fab_colorPressed="@color/wolox_secondary_blue"
        app:fab_colorRipple="@color/shakespeare"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>